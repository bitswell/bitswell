[package]
name = "claude-probe"
version = "0.1.0"
edition = "2024"
description = "Probe Claude CLI capabilities: tools, permissions, MCP servers, agents"

[[bin]]
name = "claude-probe"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
